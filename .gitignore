/Data

docs/build/

LocalPreferences.toml
Manifest.toml

/docs/build
.vscode/settings.json

# Windows "Mark of the Web" (Zone.Identifier) ADS metadata can show up as
# real files on non-NTFS filesystems after extracting/copying archives.
*:Zone.Identifier*
