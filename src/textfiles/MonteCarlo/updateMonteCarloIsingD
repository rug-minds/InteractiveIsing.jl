function updateMonteCarloIsingD(rng)
    T = TIs[]

    beta = T>0 ? 1/T : Inf
    
    idx = rand(rng, g_iterator)
    
    Estate = g.state[idx]*getEFactor(g,idx, htype)
    
    if (Estate >= 0 || rand() < exp(-beta*deltE(Estate)))
        @inbounds g.state[idx] *= -1
    end
    
end